language: c
script: make -C urbit/
notifications:
  email: false
before_install:
  - sudo apt-get update
  - sudo apt-get install -y
   libgmp3-dev libsigsegv-dev openssl libssl-dev libncurses5-dev
   make exuberant-ctags automake autoconf libtool g++ ragel cmake
   re2c libcurl4-gnutls-dev python

